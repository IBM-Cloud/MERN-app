# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
revisionHistoryLimit: 1
image:
  repository: mern-example
  tag: latest
  pullPolicy: IfNotPresent
service:
  name: Node
  type: NodePort
  containerPort: 3000
services:
  mongo:
     url: mongodb://mongo:27017
